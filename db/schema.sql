-- This minimalistic schema.sql file is simply used to create the database. In Sequelize, the models define the schema, so all the specifications necessary to structure the tables, define datatypes and other constraints, specify foreign key references, etc. is in the individual models files in the models folder. 

DROP DATABASE IF EXISTS helpfulstranger_db;
CREATE DATABASE helpfulstranger_db;

